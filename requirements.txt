# WUDAPT-FUSE — Python dependencies
# Tested on Python 3.11, Windows 11, NVIDIA RTX 3060 12 GB.
# For reproducibility we recommend creating a fresh conda env (see environment.yml).

# === Core scientific stack ===
numpy>=1.26,<2.0
pandas>=2.0
scipy>=1.11,<1.15           # 1.15 has ABI break with conda numpy 1.26
matplotlib>=3.8

# === Geospatial ===
xarray>=2024.1
rioxarray>=0.15
rasterio>=1.3
geopandas>=0.14
shapely>=2.0
osmnx>=1.9                  # OSM building footprints
cdsapi>=0.7                 # Copernicus Climate Data Store API client

# === Image / I/O ===
Pillow>=10.0                # PIL — for figure post-processing

# === Machine learning / clustering ===
scikit-learn>=1.4           # k-means + Silhouette + StandardScaler

# === Topological data analysis ===
gudhi>=3.9                  # cubical persistent homology + Wasserstein distance

# === Thermal-comfort utilities ===
pythermalcomfort>=2.10      # UTCI / PMV reference implementations

# === Manuscript build pipeline ===
python-docx>=1.1            # docx manipulation for figure embedding & SCS formatting

# === Optional: GPU acceleration ===
# Install separately to match your CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1                  # used in optional robustness checks

# === HTTP / utility ===
requests>=2.31
